
/**
 * Este programa é anotações para montar e executar os exercícios da lista de exercícios 01;
 * 
 * @Author Marnie Grenat
 * @Version16.03.2022
 */
public class notas
{
/*
 > ANOTAÇÕES DA AULA 16.03.22:
     Utilizar : int ; double ; String ; final (var). float não é recomendado, portanto não use, já que é menos preciso.
     Utilizar System.out.print/.println para apresentar a resposta do programa para discertar, Exemplo: 
     - System.out.print ("um tanananana tem:"); // System.print.ln (resposta numérica);
     No compilador, selecionar main e para executar (rechecar qual a boa disso aí);
     Caso dê overflow (explicação na lista impressa, folha 1 verso): em vez de "int", utilize "long" para executar o calculo do
programa;
     Criar um ambiente de teste para testar aplicações?;
 Erros de arredondamento:
 valores monetários não precisam ser fracionais, evita o erro acima e permite que o prog. execute com maior facilidade; (gudi gudi)
          >TIPOS NUMÉRICOS EM JAVA:
             - Tipos Inteiros: byte, short, int, long;
             - Tipos de Ponto-Flutuante: float, double;
             - Outros tipos: boolean: true OU false , char: um símbolo ou caracter na codificação Unicode;
             - Checar mais informações nas lâminas do professor (INT).
         >OPERAÇÕES BASICAS:
             - + = adi;
             - - = sub;
             _ * = multi;
             _ / = divi;
             - Ex: (a + b) / 2 ;
             - Precedências iguais.
    É mais seguro converter um valor de um tipo inteiro para um tipo de ponto-flutuante, para não perder a "precisão";
    Fazer (double -> int) is a nono;
    Imprimir valor de x com texto na frente > System.out.println("x = (TEXTO)" +i); <
    É uma boa ideia fazer as atividades no segundo monitor com as lâminas do professor no primeiro monitor;
    Exemplo:
    2 opçoes:
    d = 4.35;
    i = (int) (d * 100);
    ou 
    i = d*100;
    i= (int)d;
    
    int*double = (double)int*double, ou seja: ocorre conversão;
    
 */ 
}
